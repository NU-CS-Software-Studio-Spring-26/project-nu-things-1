<%# locals: item (RentalItem) %>
<%= render "application/user_reputation",
           user: item.user,
           empty_message: "User reputation appears once this listing is linked to a Northwestern account." %>
